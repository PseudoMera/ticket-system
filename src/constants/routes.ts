import { Route } from '../models';

export const ROUTES = {
    home: '/',
    auth: '/auth',
    fakeAuth: '/authfake',
    detail: '/detail',
    detailWithId: '/detail/:id'
};

export default ROUTES;